import { PropsTable, PropsTableRow } from "@src/components/propsTable"
import * as SharedProps from "@src/components/props"
import LiveCode from "@src/components/liveCode"
import Tabs from "@theme/Tabs"
import TabItem from "@theme/TabItem"

<Tabs
  defaultValue="data-grid-simple"
  values={[
    { label: "Data Grid – simple", value: "data-grid-simple" },
    { label: "Data Grid – image tile", value: "data-grid-image-tile" },
  ]}
>
  <TabItem value="data-grid-simple">
    <LiveCode
      code={
`<DataGrid
  entities="Article"
  itemsPerPage={50}
>
  <TextCell header="Title" field="title" />
  <TextCell header="Author" field="author.name" />
</DataGrid>`
      }
    />
  </TabItem>
  <TabItem value="data-grid-image-tile">
    <>
      <LiveCode
        code={
  `<DataGrid
    entities="Article"
    itemsPerPage={50}
    tile={<FieldView
      fields={['title', 'image.url']}
      render={(title, imageUrl) => {
        return <div style={{ borderRadius: '0.5em', overflow: 'hidden' }}>
          <img
            title={title.value}
            alt={title.value}
            src={imageUrl.value}
            style={{ verticalAlign: 'bottom' }}
          />
        </div>
      }}
    />}
    tileSize={100}
  >
    <TextCell header="Title" field="title" />
    <TextCell header="Author" field="author.name" />
  </DataGrid>`
        }
      />
      <p className="margin-top--md">You can use <a href="./field-views"><code>FieldView</code></a> for simple cases or you can <a href="/guides/writing-components/">write more complex components with static render and data accessors</a>.</p>
    </>
  </TabItem>
</Tabs>

#### Props

<PropsTable>
  <SharedProps.entities required />
  <SharedProps.itemsPerPage />
  <SharedProps.tile />
  <SharedProps.tileSize />
</PropsTable>
