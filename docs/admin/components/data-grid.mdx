---
title: Data grid
---

import { PropsTable, PropsTableRow } from "../../../src/components/propsTable"
import * as SharedProps from "../../../src/components/props"
import Tabs from "@theme/Tabs"
import TabItem from "@theme/TabItem"

import DataGrid from "./data-grid/_DataGrid.mdx"
import DateCell from "./data-grid/_DateCell.mdx"
import GenericCell from "./data-grid/_GenericCell.mdx"
import HasManySelectCell from "./data-grid/_HasManySelectCell.mdx"
import TextCell from "./data-grid/_TextCell.mdx"

## Data grid

The `DataGrid` component is a wrapper for cell components. It is used to display data in a grid.

<DataGrid />

## Date cell

The `DateCell` component adds a column for rendering date (including filtering by date range and sorting).

<DateCell />

## Generic cell

The `GenericCell` component is a generic wrapper for cell content.

<GenericCell />

## Has many select cell

The `HasManySelectCell` component adds a column for rendering has many binding.

<HasManySelectCell />

## Text cell

The `TextCell` component is a wrapper for text content.

<TextCell />
