export default ({prop, propType, description, inherited}) => (
    <tr>
        <td>
            <code>{prop}</code>
        </td>
        <td>
            {propType &&
                <p className="props-type-prop">
                    {propType}
                </p>
            }
            <p>
                {description}
            </p>
            {inherited && <span>Inherited from <code>{inherited}</code></span> }
        </td>
    </tr>
);
